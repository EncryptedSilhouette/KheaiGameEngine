using OpenTK.Windowing.Desktop;

namespace KheaiGameEngine.Core
{
    public class KWindow : GameWindow
    {
        public KWindow(GameWindowSettings windowSettings, NativeWindowSettings nativeWindowSettings) : 
            base(windowSettings, nativeWindowSettings)
        {
            
        }
    }
}
